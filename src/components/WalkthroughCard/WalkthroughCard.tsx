import { walkthroughStep } from '@/types';

const WalkthroughCard = ({ step }: { step: walkthroughStep }) => {
  return <div className='w-full h-[350px] bg-orange-500 rounded-xl'>WalkthroughCard</div>;
};

export default WalkthroughCard;
